apiVersion: 1

providers:
  - name: 'CAS Dashboards'
    orgId: 1
    folder: 'CAS Image Server'
    folderUid: 'cas-images'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
